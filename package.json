{
  "name": "server-microtasks",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "start": "docker compose -p microtasks -f docker-compose.yaml -f docker-compose.dev.yaml up --build --remove-orphans --force-recreate"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "@types/xml2js": "^0.4.14",
    "@typescript-eslint/parser": "^8.63.0",
    "bun-types": "latest",
    "eslint": "^10.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.857.0",
    "@clickhouse/client": "^1.12.0",
    "cbor-x": "^1.6.4",
    "node-cron": "^3.0.3",
    "sharp": "^0.34.3",
    "xml2js": "^0.6.2"
  }
}
